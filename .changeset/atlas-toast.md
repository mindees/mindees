---
"@mindees/atlas": minor
---

Add a **`Toast`** (Snackbar) component — a portal-backed transient notification, controlled by `visible`
with optional `duration` auto-dismiss, anchored bottom/top, `role="status"` (or `alert`). RN ships none
built-in (tenet #8).
