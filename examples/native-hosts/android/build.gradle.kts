// Root build file. Plugin versions are declared in settings.gradle.kts
// (pluginManagement), so modules apply them without a version here.
//
// ⚠️ Authored, not yet built by the maintainers — see README.md.
