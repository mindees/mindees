# Registry + network resilience (auth tokens must NEVER be committed here).
# These fetch settings make installs robust on slow/throttled networks.
registry=https://registry.npmjs.org/
fetch-retries=6
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=180000
fetch-timeout=600000
network-concurrency=4
